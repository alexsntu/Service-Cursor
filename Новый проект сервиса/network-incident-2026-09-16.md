# Плавающая недоступность dev.irepair.ru — 16 сентября 2026

## Подтверждённые наблюдения

Сервер 159.194.210.246, Beget. Пользователь сообщает о повторениях весь день у разных пользователей; текущая сеть пользователя без Podkop. Старое объяснение домашним Podkop нельзя считать объяснением этого инцидента.

- Около 16:20 UTC (19:20 МСК): прямое HTTPS-соединение с явной привязкой к Wi-Fi 192.168.1.42 устанавливает TCP, но зависает на TLS ClientHello. HTTP/80 отвечает 301; SSH/22 работает; HTTPS панели на 1500 отвечает 200 (для панели сертификат в диагностике не проверялся).
- Обычный curl/SSH без привязки к Wi-Fi дополнительно получает локальную ошибку Can't assign requested address. На Mac default идёт через utun4, для VPS задан host-route через en0/192.168.1.1. Привязка исходного адреса устраняет эту ошибку, но НЕ устраняла TLS timeout. Это отдельное локальное наблюдение, не объяснение жалоб всех пользователей.
- На VPS nginx, php8.3-fpm, mysql активны, load average около 0.02, доступно около 10.6 ГБ RAM, swap не используется, диск занят на 7%. Проверка сайта с VPS: HTTPS 200, 0.23 с.
- Во время прямого TLS timeout тот же HTTPS через временный SSH-туннель с проверкой сертификата: 200, 0.51 с, 207784 байта. Туннель затем закрыт.
- В серверном tcpdump 16:20:37–16:20:43 UTC контрольный поток от 92.51.12.42:62993 устанавливает TCP, но payload ClientHello не виден до конца записи. Другие потоки с этого IP частично передавали данные; нельзя распространять отсутствие payload на все соединения. Локальная запись пакетов недоступна без sudo-пароля, поэтому выход ClientHello с физического интерфейса не подтверждён.
- nginx -t успешен; в journalctl ядра и nginx/php/mysql за два часа нет warning/error. В файловом логе сайта есть предупреждения о буферизации больших ответов на диск; они не доказывают причину TLS timeout.
- Цепочки ispmgr_deny_ip/ispmgr_deny_sub пусты; ispmgr_limit_req содержит 0 адресов (проверено во время расследования); ispmanager-ddos inactive; fail2ban имеет только sshd jail. Это не проверка внешней защиты Beget.
- Около 16:22–16:23 UTC Check-Host получил HTTPS 200 из Болгарии, Финляндии, Ирана, Нидерландов, Португалии, Москвы и Петербурга. Это снимок доступности в момент измерений, не доказательство отсутствия сбоя раньше или позже.
- Около 16:23 UTC прямой HTTPS восстановился, что подтвердил пользователь. Чередующиеся default TLS / TLS 1.2 все успешны; нельзя приписывать восстановление принудительному TLS 1.2.
- Службы не перезапускались: nginx/PHP active с 07:29:06 UTC, MySQL с 07:29:08 UTC. Конфигурация в расследовании не менялась, перезагрузок не было.

Внешние отчёты:
- https://check-host.net/check-report/4bd064f2k89e
- https://check-host.net/check-report/4bd066ddk6b2

## Предварительный вывод

Наблюдаемый сбой находится до обработки HTTP-запроса CS-Cart. Недостаток ресурсов/перезапуск приложения не подтверждён. Основное направление — нестабильность публичного TCP/443/TLS по некоторым сетевым путям: фильтрация на клиенте/в сети, операторский маршрут либо внешняя защита/сеть хостинга. Конкретный виновник пока не установлен. Доказательств универсальной пятиминутной периодичности пока нет.

## Наблюдение

С 16:24 UTC запущено 22 измерения с интервалом 20 секунд: прямой HTTPS с Wi-Fi и серверный HTTPS через SSH. Результаты: `network-monitor-2026-09-16.jsonl`. Каждая запись содержит точное время UTC, exit code, HTTP/TCP/TLS timing и состояние uptime сервера.

## Черновик обращения в Beget (не отправлен)

На VPS 159.194.210.246 периодически недоступен публичный HTTPS dev.irepair.ru. Просьба проверить внешнюю anti-DDoS/ACL/фильтрацию и маршрут на TCP/443, включая журналы событий и лимитов для IP сервера. Пример: 16.09.2026 около 16:20:37–16:20:43 UTC, источник 92.51.12.42, контрольный TCP source port 62993. TCP handshake завершается, но TLS ClientHello не наблюдается на eth0 VPS, хотя клиент сообщает начало отправки. В это время HTTP/80 и SSH/22 работают; HTTPS с самого VPS и через SSH-туннель возвращает 200 менее чем за секунду. Nginx/PHP/MySQL активны, сервер не перегружен, локальные списки блокировок пусты. Около 16:23 UTC прямой HTTPS восстановился без каких-либо изменений или рестартов. Нужна проверка сетевого уровня и внешней фильтрации в указанный интервал, а не только текущий ping или статус веб-сервера. Наличие сбоя у всех сетей одновременно не подтверждено: внешние пробы около восстановления получили 200.


## Итог наблюдения и уточнения (16:34 UTC)
22 парных измерения, 16:24:05–16:31:12 UTC: прямой HTTPS 11 успехов, затем 11 последовательных TLS timeout с 16:27:45 UTC; с VPS все 22 проверки HTTP 200. Последний успех до сбоя 16:27:25 UTC. Пользователь подтверждает повторные отключения и подчёркивает одинаковую картину у всех проверенных им пользователей; не повторять вопросы об этом.
Во время повторного сбоя внешние узлы вернули HTTP 200: Москва 0.31с, СПб 0.35с, Финляндия 0.58с, Нидерланды 9.55с. Отчёт https://check-host.net/check-report/4bd09cbckbc3 . Причина большой задержки NL не установлена. На 16:30:03 UTC локальные deny chains пусты и ispmgr_limit_req содержит 0 адресов. Периодическая недоступность подтверждена, регулярность каждые 5 минут ещё не доказана.
DNS с VPS: A 159.194.210.246, AAAA/CNAME не возвращены, NS ns1.reg.ru/ns2.reg.ru. Стандартное Cloudflare proxy перед доменом не обнаружено (проверенные обращения идут на origin IP). Пользователь сообщил постоянную недоступность Safari на iPhone/Mac. После предоставления UI-доступа Safari Mac содержал DOM страницы, а снимок показывал полноценный баннер и меню при незавершённом индикаторе загрузки. Это не доказывает исправность Safari во все моменты; повторяемая браузерная причина/Service Worker не диагностирована. UI-инструмент крайне медленный (~73–78с на вызов), iPhone не проверен. Никаких настроек Safari, DNS, маршрутов, VPS или приложения не меняли.
Следующий обоснованный шаг: сетевые журналы/anti-DDoS Beget для двух интервалов (16:20 и 16:27:45–16:31:12 UTC), синхронный клиентский/серверный capture при повторе. Полноценный клиентский capture требует доступного sudo. Черновик обращения выше дополнить вторым интервалом и отчётом внешних узлов. Отправка в поддержку не выполнялась.


## КОНТРОЛЬНЫЙ ТЕСТ: статическая заглушка ВКЛЮЧЕНА 2026-09-16 16:35 UTC
По прямой просьбе пользователя временно исключить CS-Cart создан `/etc/nginx/vhosts-resources/dev.irepair.ru/01-static-diagnostic.conf`: server-level `return 200` со статическим HTML, default_type text/html, Cache-Control no-store и X-iRepair-Diagnostic static-20260916. Это ответ до PHP для ВСЕХ путей в обоих server-блоках домена, включая /index.php и админку. На странице текст «Временная техническая страница», marker STATIC-DIAGNOSTIC-20260916. Файлы CS-Cart, база, сервис PHP не удалены/не остановлены. nginx -t успешен, выполнен только reload Nginx. Backup vhost/resources: `/root/irepair-diagnostic-backup-20260916T163444Z/` (вне webroot).
Результат: 3 прямые проверки заглушки с Mac (последние 16:36:26 и 16:36:46 UTC) получили TCP connect ~0.05s и TLS timeout ~5s. В то же время серверный HTTPS /index.php вернул 200 за 0.0314s. Значит, обработка CS-Cart/PHP не нужна для воспроизведения текущего TLS-сбоя. Причина сетевого/TLS обрыва остаётся не установлена; не заявлять, что определён конкретный провайдер/защита.
ЗАГЛУШКА ОСТАВЛЕНА ВКЛЮЧЁННОЙ для наблюдения пользователя. Восстановление: убрать из include только 01-static-diagnostic.conf (переместить в backup с расширением не .conf в include-директории), nginx -t и systemctl reload nginx. Готовый локальный rollback-скрипт `/private/tmp/irepair-disable-static.sh`, НЕ выполнен. Следующие агенты должны знать: сейчас витрина и админка намеренно заменены заглушкой. TLS/сертификат/маршруты/firewall не менялись. Диагностика Safari теперь возможна только как тест заглушки; исходную страницу проверять после согласованного возврата.


### Static-page recovery confirmed 2026-09-16 16:38:09 UTC (19:38:09 MSK)
User reported availability restored. Direct curl bound to Wi-Fi confirmed HTTP 200, TLS 0.105143s, total 0.149060s, X-iRepair-Diagnostic static-20260916 and body marker STATIC-DIAGNOSTIC-20260916. No changes/reloads since static switch at 16:35 UTC. This confirms failure then recovery while the Nginx static placeholder stays enabled, without CS-Cart request processing. Exact network/filter cause still unproven; placeholder remains active.
